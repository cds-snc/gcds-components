import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as BreadcrumbsItem from './gcds-breadcrumbs-item.stories';

<Meta of={BreadcrumbsItem} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={BreadcrumbsItem.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={BreadcrumbsItem.Props} sort="requiredFirst" />
