import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as NavLink from './gcds-nav-link.stories';

<Meta of={NavLink} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={NavLink.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={NavLink.Props} sort="requiredFirst" />