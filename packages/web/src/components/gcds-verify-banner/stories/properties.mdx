import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as VerifyBanner from './gcds-verify-banner.stories';

<Meta of={VerifyBanner} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={VerifyBanner.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={VerifyBanner.Props} sort="requiredFirst" />
