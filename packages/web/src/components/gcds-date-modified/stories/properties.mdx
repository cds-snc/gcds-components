import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as DateModified from './gcds-date-modified.stories';

<Meta of={DateModified} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={DateModified.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={DateModified.Props} sort="requiredFirst" />
