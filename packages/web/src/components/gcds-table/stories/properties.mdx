import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Table from './gcds-table.stories';

<Meta of={Table} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={Table.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Table.Props} sort="requiredFirst" />
