import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Notice from './gcds-notice.stories';

<Meta of={Notice} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={Notice.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Notice.Props} sort="requiredFirst" />
