import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as ErrorMessage from './gcds-error-message.stories';

<Meta of={ErrorMessage} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={ErrorMessage.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={ErrorMessage.Props} sort="requiredFirst" />
