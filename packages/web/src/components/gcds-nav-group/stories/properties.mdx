import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as NavGroup from './gcds-nav-group.stories';

<Meta of={NavGroup} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={NavGroup.Default}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={NavGroup.Default} sort="requiredFirst" />