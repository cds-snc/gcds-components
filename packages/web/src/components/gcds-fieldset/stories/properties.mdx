import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Button from './gcds-fieldset.stories';

<Meta of={Button} name="Events & properties" />

# Events & properties

<Canvas
  of={Button.Default}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Button.Default} sort="requiredFirst" />
