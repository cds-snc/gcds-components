import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Grid from './gcds-grid.stories';

<Meta of={Grid} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={Grid.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Grid.Props} sort="requiredFirst" />
