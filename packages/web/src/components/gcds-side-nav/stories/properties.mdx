import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as SideNav from './gcds-side-nav.stories';

<Meta of={SideNav} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={SideNav.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={SideNav.Props} sort="requiredFirst" />