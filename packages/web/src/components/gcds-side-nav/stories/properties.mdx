import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as SideNav from './gcds-side-nav.stories';

<Meta of={SideNav} name="Events & properties" />

# Events & properties

<Canvas
  of={SideNav.Default}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={SideNav.Default} sort="requiredFirst" />