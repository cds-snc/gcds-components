import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Heading from './gcds-heading.stories';

<Meta of={Heading} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={Heading.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Heading.Props} sort="requiredFirst" />
