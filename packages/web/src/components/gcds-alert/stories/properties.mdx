import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Alert from './gcds-alert.stories';

<Meta of={Alert} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={Alert.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Alert.Props} sort="requiredFirst" />
