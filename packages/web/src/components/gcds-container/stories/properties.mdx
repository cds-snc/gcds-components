import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Container from './gcds-container.stories';

<Meta of={Container} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={Container.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Container.Props} sort="requiredFirst" />
