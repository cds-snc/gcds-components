import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Pagination from './gcds-pagination.stories';

<Meta of={Pagination} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={Pagination.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Pagination.Props} sort="requiredFirst" />
