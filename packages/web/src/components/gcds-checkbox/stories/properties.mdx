import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Checkbox from './gcds-checkbox.stories';

<Meta of={Checkbox} name="Events & properties" />

{!(new URLSearchParams(window.location.search)).get('demo') && <h1>Events & properties</h1>}

<Canvas
  of={Checkbox.Props}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Checkbox.Props} sort="requiredFirst" />
