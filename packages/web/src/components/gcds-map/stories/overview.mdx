import { Meta, Story } from '@storybook/blocks';
import * as Map from './gcds-map.stories';

<Meta title="GCDS Map Overview" />

# GCDS Map Overview

The `gcds-map` component is a wrapper around the `mapml-viewer` custom element and allows you to dynamically manage map layers.

<Story name="Default">
  <Map.Default />
</Story>
