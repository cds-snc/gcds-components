import { Meta, Canvas, Story } from '@storybook/blocks';
import * as Map from './gcds-map.stories';

<Meta of={Map} name="Map" />

# Map<br/>`<gcds-map>`

The `<gcds-map>` component enables slipply maps with one or more `<gcds-layer>` children

<Canvas>
  <Story name="Default">
    <Map.Default />
  </Story>
</Canvas>
