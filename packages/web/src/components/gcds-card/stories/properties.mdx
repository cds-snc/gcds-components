import { Meta, Canvas, Controls } from '@storybook/blocks';
import * as Card from './gcds-card.stories';

<Meta of={Card} name="Events & properties" />

# Events & properties

<Canvas
  of={Card.Default}
  story={{ inline: true }}
  sourceState="shown"
  type="dynamic"
/>

<Controls of={Card.Default} sort="requiredFirst" />
